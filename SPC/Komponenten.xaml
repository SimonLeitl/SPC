<UserControl x:Class="SPC.Komponenten"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SPC"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="150">
    <Grid>
        <ListBox BindingGroup="{Binding Komponents}">
            <!-- ItemsSource="{StaticResource Komponenten}" -->
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Vertical">

                    </StackPanel>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBoxItem>
                <TextBlock>Test des Liste</TextBlock>
            </ListBoxItem>
            <ListBoxItem>
                <TextBlock>Test des 2. Listenelement</TextBlock>
            </ListBoxItem>
        </ListBox>

    </Grid>
</UserControl>
